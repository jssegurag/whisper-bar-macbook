# Binarios compilados (generados por build.sh y por el arnés de UI)
Gluffi_bin
WhisperBar_bin
PreviewUI

# macOS
.DS_Store
*.swp

# Xcode (si se abre con Xcode en el futuro)
*.xcodeproj/
xcuserdata/
DerivedData/
