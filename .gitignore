# Binarios compilados (generados por build.sh y por el arnés de UI)
Gluffi_bin
WhisperBar_bin
PreviewUI

# macOS
.DS_Store
*.swp

# Xcode (si se abre con Xcode en el futuro)
*.xcodeproj/
xcuserdata/
DerivedData/

# Binario de los tests (run_tests.sh lo borra al terminar; queda si el build falla)
WhisperBar_tests
